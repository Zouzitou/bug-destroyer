# Secrets / local config
.env
.env.*
.env.local
*.pem
*.key
*.crt
*.p12
credentials*
secret*
config.local.*
.aws/
.netrc
*.log

# Raw scan artifacts (may contain unredacted findings)
bugs_raw_agent_*.jsonl

# Keep the skill descriptor versioned; exception must precede the directory ignore
!.claude-plugin/plugin.json

# Claude-specific directories
.claude/
.claude-plugin/

# Build outputs
builds/
dist/
